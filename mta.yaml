_schema-version: "3.1"
ID: FIEM-report
version: 1.0.0

modules:

  - name: FIEM-report
    type: html5
    path: .
    build-parameters:
      builder: custom
      commands:
        - npm install
        - npm run build
      build-result: approuter/resources
      supported-platforms: []   

  - name: FIEM-report-approuter
    type: approuter.nodejs
    path: approuter
    parameters:
      memory: 256M
      disk-quota: 512M
    requires:
      - name: destination-report-service
      - name: connectivity-report-service
      - name: xsuaa-report-service
    provides:
      - name: approuter-api
        properties:
          url: ${default-url}

resources:

  - name: destination-report-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite

  - name: connectivity-report-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite

  - name: xsuaa-report-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json